# --- InsightFlow Configuration ---

# 1. Google Gemini API Keys (Multi-Tier Strategy)
# Separate multiple keys with commas (key1,key2,key3)
# Get keys at: https://aistudio.google.com/

# Tier 1: Free Keys (Billing Disabled). Used first.
# Example: GOOGLE_KEYS_FREE=AIzaSyD...,AIzaSyB...
GOOGLE_KEYS_FREE=GOOGLE_KEYS_FREE_1, GOOGLE_KEYS_FREE_2

# Tier 2: Paid Keys (Billing Enabled). Used only when Free keys are exhausted.
GOOGLE_KEYS_PAID=GOOGLE_KEYS_PAID

# Target Model (Optional). Overrides auto-detection.
# Options: gemini-2.0-flash, gemini-2.0-flash-lite, gemini-1.5-flash
GOOGLE_MODEL=gemini-2.0-flash-lite

# --- 2. Directories Configuration ---
# You can override default paths here. 
# Use absolute paths for stability on Windows.

# Where to look for local files to process (The Drop Folder)
INSIGHTFLOW_INBOX=C:\Users\One\Downloads\InsightFlowInbox

# Where to store standardized audio files internally
INSIGHTFLOW_INPUT=C:\Dev\Python\InsightFlow\input

# Where to save analysis results (folders with MD files)
INSIGHTFLOW_OUTPUT=C:\Dev\Python\InsightFlow\output

# Path to sibling project if not default ../YTConverter
YTCONVERTER_PATH=C:\Dev\Python\YTConverter

# --- 3. Future-Proofing / Optional Modules ---

# OpenAI API (For alternative analysis/transcription in Phase 3+)
OPENAI_API_KEY=OPENAI_API_KEY_VALUE

# Local Whisper Settings (For Phase 3 Offline Mode)
# Device: 'cuda' (Nvidia GPU), 'cpu', or 'mps' (Mac)
# WHISPER_DEVICE=cuda
# Model: 'tiny', 'base', 'small', 'medium', 'large-v3'
# WHISPER_MODEL=base
